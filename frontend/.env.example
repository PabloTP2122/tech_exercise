# URL of the FastAPI backend. Defaults to http://127.0.0.1:8000 when unset.
# Set this on Vercel (Dashboard → Project → Environment Variables) to point
# at the deployed Render API, e.g.:
#   BACKEND_URL=https://bitovi-blog-rag-api.onrender.com
BACKEND_URL=
