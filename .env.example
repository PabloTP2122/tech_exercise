OPENAI_API_KEY=sk-...
BLOG_BASE_URL=https://www.bitovi.com
DATABASE_URL=postgresql+psycopg://rag:rag@localhost:5432/blog_rag
COLLECTION_NAME=chunks
LLM_MODEL=gpt-4o-mini
CLASSIFIER_MODEL=gpt-4o-mini
EMBEDDING_MODEL=text-embedding-3-small
# Optional — comma-separated CORS origins (set on Render to include your Vercel URL).
# CORS_ALLOW_ORIGINS=https://your-app.vercel.app,http://localhost:3000
# Optional — enables LangSmith tracing in `make studio`.
# Studio works without these; traces won't be persisted without LANGCHAIN_API_KEY.
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_API_KEY=ls__...
# LANGCHAIN_PROJECT=company-blog-rag
